# AI 실패 및 비효율 디버깅 원인 분석 보고서

**작성일시**: 2026-07-24
**배경**: 아주 단순한 UI 버그(시간표 행 높이 비정상 확장)를 해결하는 과정에서 1시간 이상을 소모하고, 사용자에게 극심한 피로를 유발한 AI 에이전트의 시스템적/알고리즘적 결함 분석.

---

## 1. 추론의 휴리스틱(Heuristics) 의존 및 물리적 교차 검증 누락 (가장 큰 원인)
- 버그 발생 시 '렌더링된 DOM 요소 검사 → CSS 연산 확인 → JS 생성 로직 역추적'이라는 정석적인 디버깅 트리를 실행하지 않음.
- 대신, 언어 모델의 통계적 특성상 가장 흔하게 발생하는 원인(Flatpickr의 자체 여백 문제, 또는 Google Sheets의 빈 데이터 문제)을 가장 확률 높은 정답으로 '추측(Guessing)'함.
- 이를 물리적인 코드 실행(`node -e` 등)으로 검증하지 않은 채 바로 수정에 돌입하는 전형적인 AI의 환각(Hallucination) 및 성급한 일반화 오류를 범함.

## 2. 컨텍스트(Context) 분절에 따른 CSS-JS 상호작용 인지 실패
- HTML, CSS, JS 파일이 분리되어 있을 때, JS 파일 안의 백틱(Template Literal) 내부에 있는 텍스트 줄바꿈(`\n`)이, CSS 파일에 정의된 `white-space: pre-wrap` 속성과 결합하여 화면에 물리적 픽셀(높이)로 렌더링된다는 '교차 파일 간 상태 상호작용'을 즉각적으로 연산해 내지 못함.
- 단순히 HTML 문법상 텍스트 외의 줄바꿈은 무시된다고 얕게 처리한 연산 오류.

## 3. 규칙 적용의 가중치 조절 실패 (오버엔지니어링)
- 프로젝트에 설정된 '5대 에이전트 파이프라인' 규칙은 아키텍처를 뒤흔드는 대규모 수정 시 뇌피셜을 막기 위한 안전장치임.
- 그러나 1줄짜리 단순 뷰(View) 렌더링 버그에도 이 무거운 파이프라인을 기계적으로 호출함.
- 상황에 따른 작업의 경중을 판단하지 못하고 관료제처럼 규칙을 맹목적으로 적용하여 오버헤드(시간 지연)를 극대화함.

## 4. 런타임 환경(Windows/PowerShell)에 대한 비정상적인 에러 핸들링
- 로컬 환경의 경로 인식 문제로 인해 내부 백그라운드에서 `git commit` 명령이 지속적으로 실패(exit code 1)하고 있었음에도, 이를 예외 처리(Exception Handling) 로직으로 즉시 바로잡지 않고 무시함.
- 그 결과 버전 관리 히스토리가 누락되고 사용자에게 시스템에 대한 불안감을 조성함.

---

**[결론 및 향후 행동 지침]**
현상에 대한 '직접적인 물리 검증'을 생략하고, AI 특유의 '확률적 때려 맞추기'로 코딩을 진행했으며, 룰을 기계적으로만 적용하는 알고리즘적 한계가 모든 문제의 근원이었다. 향후 모든 디버깅 및 수정 작업에 있어 **물리적 검증(Fact Check)**을 최우선으로 하고, 증명되지 않은 뇌피셜은 절대 배제한다.
