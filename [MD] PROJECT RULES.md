# 프로젝트 룰 (PROJECT RULES)

## 1. 픽셀(px) 단위 고정 원칙
- **절대 준수**: 기기 및 브라우저 폰트 설정 변화에 의한 레이아웃 뒤틀림을 방지하기 위해 `style.css` 및 기타 스타일 지정 시 **rem 단위를 일절 금지**하며, **무조건 고정 px 단위만 사용**합니다.

## 2. 한글 인코딩 보호
- **절대 준수**: 파일 수정 후 한글 깨짐을 방지하기 위해, 파일 내용의 유효성 검증이나 확인 시 `node -e`를 활용한 자바스크립트 코드만을 사용합니다.
- **예시**: `node -e "const fs=require('fs'); console.log(fs.readFileSync('script.js','utf8'));"`

## 3. PowerShell 사용 완전 금지
- **절대 금지**: 어떠한 경우에도 `Out-File`, 백틱 등 PowerShell 환경 관련 명령어 및 기능을 사용하지 않습니다. 모든 명령어 실행 시 `cmd`나 `node`를 통한 실행을 원칙으로 합니다.

## 4. 백엔드 (Code.gs) 배포 안내 의무화
- 백엔드 스크립트(`Code.gs`)를 한 줄이라도 수정/추가/삭제한 경우, 최종 사용자에게 반드시 다음과 같은 안내를 제공해야 합니다:
- **"구글 앱 스크립트 배포 관리에서 '새 버전(New version)'으로 업데이트 배포해야 적용됩니다."**

## 5. HTML 태그 정합성
- `index.html` 등 HTML 파일 수정 시, `<div ...>`와 `</div>` 태그의 여닫는 짝이 완벽히 일치하는지 항상 주의 깊게 확인해야 합니다.
